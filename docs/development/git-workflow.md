# Git 工作流

- 从目标分支创建短生命周期特性分支，提交保持单一目的。
- 不提交 `target/`、本地 IDE 状态、真实配置凭据、密钥或日志数据。
- 安全协议、路由语义、同步数据源或公共配置变化应在同一变更中更新项目 docs。
- 生成镜像前先单独执行测试；`build.sh` 当前会跳过测试。
- 提交前检查 `git diff`、`git diff --check` 和未跟踪文件，避免混入旧备份或临时产物。
- README 更新使用独立 `generate` 命令，不与 `init` 混合。
