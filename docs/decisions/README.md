# 项目决策

当前没有项目级 ADR。

以下变化应新增 ADR：

- Gateway 中央 Profile 的采用或本地基线替换；
- 认证方案、过滤器顺序、白名单或可信主体头的长期变化；
- 路由/权限主数据源、同步机制或缓存一致性模型变化；
- WebFlux 与 WebMVC 网关的职责分工或兼容策略；
- 日志、签名、Token 密钥轮换和失败策略的重大调整。

ADR 记录状态、背景、决策、替代方案、兼容性、安全影响、迁移、验证和回滚。
