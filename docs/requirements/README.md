# 需求索引

当前没有已登记的活动需求。

新增需求时，每个需求文档至少包含：状态、背景、范围、非目标、接口/安全/兼容性影响、验收标准、测试计划和文档影响。只有一个需求被明确标记为 `active` 后，才能使用 `develop g2rain-gateway-webflux` 执行开发。

优先候选事项可从[架构偏差与已知风险](../architecture/deviations.md)建立正式需求，但不得仅凭风险表自动选择开发任务。
